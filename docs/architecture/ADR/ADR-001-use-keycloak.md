# ADR-001: Use Keycloak

Accepted. IITD IAM uses Keycloak for OAuth 2.0, OIDC, password handling, MFA, sessions, token signing and federation. This avoids custom protocol and cryptographic implementation risk.

