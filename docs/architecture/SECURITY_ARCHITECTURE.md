# Security Architecture

Security controls include PostgreSQL-only persistence, strict redirect URI validation, backend permission enforcement, no plaintext invitation token storage, no custom cryptography, server-only Keycloak admin credentials, and structured audit events that exclude secrets.

