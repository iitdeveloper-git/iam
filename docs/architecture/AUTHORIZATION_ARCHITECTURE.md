# Authorization Architecture

Backend authorization is authoritative. Frontend permission checks are UX-only. Platform roles map to permissions, and assignment rules prevent lower-ranked administrators from assigning higher-ranked platform roles.

