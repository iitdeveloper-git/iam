# Deployment

Production deployment requires TLS, managed PostgreSQL, managed Redis, restricted Keycloak admin access, a secret manager, migration jobs, health checks, logging, metrics and rollback procedures.

