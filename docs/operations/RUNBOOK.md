# Runbook

Check `/health/live`, `/health/ready`, `/metrics`, container status, database connectivity, Redis connectivity and Keycloak reachability. Escalate if authentication, token verification or admin workflows fail.

