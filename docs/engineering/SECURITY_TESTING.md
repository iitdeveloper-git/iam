# Security Testing

Required tests include invalid issuer, invalid audience, expired token, future not-before, unsupported algorithm, forged token, redirect abuse, invitation replay, lower-admin privilege escalation, suspended user access, secret leakage, CSRF, CORS and XSS.

