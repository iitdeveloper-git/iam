# Frontend Architecture

The admin console uses Next.js, TypeScript and Tailwind. It is organized around operational pages and a shared shell. Permission checks in the UI must never replace backend enforcement.

